/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['lh-web-s3-bucket.s3.eu-west-2.amazonaws.com'], // Replace with your image domain
    },
};

export default nextConfig;
